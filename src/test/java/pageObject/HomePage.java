package pageObject;import base.BasePage;import org.openqa.selenium.By;import org.openqa.selenium.WebDriver;import utils.ElementUtils;public class HomePage extends BasePage {    //接收外来的driver    public HomePage(WebDriver driver){        super(driver);    }    //定位元素 locator    //private static final By MYACCOUNT_LINK = By.xpath("//span[normalize-space()='My Account']");   // 写在basepage    // 里了, 不需要在每个页面都写    private static final By REGISTER_LINK = By.linkText("Register");    private static final By LOGIN_LINK = By.xpath("//a[normalize-space()='Login']");    //点击 My Account ->Register    public void clickRegister(){        elementUtils.click(MYACCOUNT_LINK);        elementUtils.click(REGISTER_LINK);    }    //点击 My Account -> Login, 放到basePage 里了//    public void clickLogin(){//        elementUtils.click(MYACCOUNT_LINK);//        elementUtils.click(LOGIN_LINK);//    }}