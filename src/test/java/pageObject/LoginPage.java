package pageObject;import base.BasePage;import org.openqa.selenium.By;import org.openqa.selenium.WebDriver;import org.openqa.selenium.support.ui.ExpectedConditions;import org.openqa.selenium.support.ui.WebDriverWait;import utils.ElementUtils;import java.time.Duration;public class LoginPage extends BasePage {    //继承 BasePage，通过构造器接收driver 并将driver传给base page    public LoginPage(WebDriver driver) {        super(driver);    }    //定位元素 locator    private static final By EMAIL_ADDRESS_LOGIN = By.id("input-email");    private static final By PASSWORD_LOGIN = By.id("input-password");    private static final By BUTTON_LOGIN = By.cssSelector("button[type='submit']");    private static final By FORGETPASSWORD_LOGIN = By.linkText("Forgotten Password");    private static final By MY_ACCOUNT_TEXT = By.xpath("//h2[normalize-space()='My Account']");    private static final By WARN_MSG = By.xpath("//dirv[@class='alert alert-danger alert-dismissible']");    //login 方法    public void logIn(String email, String password){        elementUtils.sendKeys(EMAIL_ADDRESS_LOGIN,email);        elementUtils.sendKeys(PASSWORD_LOGIN,password);        elementUtils.click(BUTTON_LOGIN);      //  elementUtils.waitForVisible(MY_ACCOUNT_TEXT);    }    //forget password 方法    public void forgetPasswordLogin(By locator){      elementUtils.click(FORGETPASSWORD_LOGIN);    }    public String getWarnMessage(){        String message = elementUtils.waitForVisible(WARN_MSG).getText();        return message;    }}